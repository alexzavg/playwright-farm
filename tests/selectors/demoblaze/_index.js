const { HomeSelectors } = require('./home');
const { ProductSelectors } = require('./product');
const { CartSelectors } = require('./cart');
const { CheckoutSelectors } = require('./checkout');
const { ConfirmationSelectors } = require('./confirmation');

module.exports = { HomeSelectors, ProductSelectors, CartSelectors, CheckoutSelectors, ConfirmationSelectors };
